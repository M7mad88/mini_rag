version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: always

    ports:
      - "28017:27017"   # host:container

    volumes:
      - ./mongodb:/data/db

    networks:
      - backend

networks:
  backend:
